const styles = ({ palette }) => {
  return {
    videoContainer: {
      width: '100%',
      height: '40vh'
    },
    video: {
      width: '100%',
      height: '100%'
    }
  }
}

export default styles
